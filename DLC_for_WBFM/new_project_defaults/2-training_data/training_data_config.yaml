# Default parameters for creating a new project
# Do not edit!

self_path: 2-training_data/training_data_config.yaml # for programmatic updating

#########################
# Tracking settings
#########################

preprocessing_config: 2-training_data/preprocessing_config.yaml

tracker_params: # TODO: OPTIMIZE
  neuron_feature_radius: 5.0
  use_affine_matching: false
  add_affine_to_candidates: true
  add_gp_to_candidates: true
  save_candidate_matches: true
  external_detections: null # Will be synchronized with segmentation
