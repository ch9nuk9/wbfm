# Default parameters for creating a new project
# Do not edit!
# NOTE: these comments will be deleted when moving to a new project!

task_name: test
experimenter: Charlie

project_dir: null # For new project
project_path: null # For reference afterwards
###################
# Initialization
###################
red_bigtiff_fname: null
green_bigtiff_fname: null

# For now: Preprocessing is a separate file (shared between packages)
# preprocessing_config: new_project_defaults/preprocessing_config.yaml

subfolder_configs:
  # Copy from external package
  segmentation: segmentation/segment_config.yaml
  # Outputs of this package
  training_data: training_data/training_data_config.yaml
  tracking: tracking/tracking_config.yaml
  traces: traces/traces_config.yaml

###################
# Project status
###################
current_status:
  segmentation: false
  training_data_3d: false
  training_data_2d: false
  dlc_networks: false
  traces: false

###################
# Other
###################
other:
  verbose: 2
  DEBUG: false
